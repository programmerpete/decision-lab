/**
 * The deployed live endpoint.
 *
 * A public URL, not a credential. It is repeated here so the smoke script does not have
 * to import a TypeScript module from plain Node.
 */
export const LIVE_ENDPOINT = 'https://decision-lab-live.petersk.workers.dev';
